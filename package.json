{
  "name": "temperature-monitor",
  "version": "1.0.0",
  "private": true,
  "description": "Express + MQTT + Socket.IO temperature and humidity dashboard",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.21.2",
    "mqtt": "^5.1.0",
    "socket.io": "^4.8.1"
  }
}
